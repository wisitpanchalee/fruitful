import { StubPage } from '@/components/stub-page';
export const runtime = 'edge';
export default function Page() {
  return <StubPage title="ติดตามสต็อก" subtitle="รายการสต็อกตามผลไม้ / ล็อต / ของเสีย / audit log" />;
}
